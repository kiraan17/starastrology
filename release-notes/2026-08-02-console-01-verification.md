# CONSOLE-01 — Internal verification console

Plain FastAPI + HTML form for engine inspection (chart, Parashara, KP, Ashtakavarga) with JSON export.

Run: `uvicorn console.app:app --app-dir .`

`pytest`: 56 passed.

Not a customer UI.
